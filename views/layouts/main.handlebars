<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doing the Code Thing</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/Assets/public/css/style.css">
</head>
<body>
    <div>
        <header class="container mx-auto pt-5 pb-5">   
            <h1 class="text-4xl font-bold pb-4">
                Doing the Code Thing!
            </h1>
            <nav>
                <a class="bg-indigo-600 p-3 mr-2 rounded-md" href="/">Home</a>
                <a class="bg-indigo-600 p-3 mr-2 rounded-md" href="/dashboard">Dashboard</a>
                {{#if logged_in}}
                <button class="no-button bg-indigo-600 p-3 rounded-md" id="logout">logout</button>
                {{else}}
                <a  class="bg-indigo-600 p-3 rounded-md" href="/login">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            <!-- Render the sub layout -->
            {{{ body }}}
        </main>
    </div>

    <!-- if user is logged in, include script for logout js -->
    {{#if logged_in}}
    <script src="js/logout.js"></script>
    {{/if}}
</body>
</html>